{
  "name": "fishjam-web-sdk",
  "version": "0.4.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/ts-client",
    "packages/react-client",
    "examples/ts-client/*",
    "examples/react-client/*",
    "e2e-tests/ts-client/app",
    "e2e-tests/react-client/app"
  ],
  "packageManager": "yarn@4.3.0",
  "scripts": {
    "build": "yarn workspace @fishjam-dev/ts-client build && yarn workspace @fishjam-dev/react-client build",
    "build:e2e": "yarn workspace @fishjam-dev/ts-client-e2e build && yarn workspace @fishjam-dev/react-client-e2e build",
    "test:e2e": "yarn workspace @fishjam-dev/ts-client-e2e e2e && yarn workspace @fishjam-dev/react-client-e2e e2e",
    "tsc": "yarn workspaces foreach -A -p run tsc",
    "format": "yarn workspaces foreach -A -p run format",
    "format:check": "yarn workspaces foreach -A -p run format:check",
    "lint": "yarn workspaces foreach -A -p run lint",
    "lint:check": "yarn workspaces foreach -A -p run lint:check"
  }
}
